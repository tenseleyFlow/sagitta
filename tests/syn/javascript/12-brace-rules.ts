const all = `${{{ @dec interface public satisfies string Type <T extends X> /* block */ "s" 's' `nested` /re/ + this return if const 0xff 0b1 0o7 1.2 .5 call(x) .method id ++ => (x), . }}}`;
const comment = `${{{ // line comment
value }}}`;
