import io

let lsp = {
    servers: {
        c: {
            id: "fakelsp",
            cmd: io.env("YEW_TEST_FAKELSP"),
            args: ["session-sync"],
            roots: [".git"],
            init_options: nil,
            init_timeout_ms: 2000,
        },
    },
}
